<%- include('partials/navigation.ejs') %>
<h1>Authors</h1>
<ul>
    <% if (authors.length > 0) { %>
        <% authors.forEach(author => { %>
            <li><%= author.name %></li>
        <% }) %>
    <% } else { %>
        <p>No authors have been found.</p>
    <% } %>
</ul>
